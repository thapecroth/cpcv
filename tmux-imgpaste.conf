# imgpaste tmux bindings (source from ~/.tmux/.tmux.conf.local)
# prefix + I  => insert path of latest uploaded clipboard image into pane
# prefix + C-i => same

bind-key I run-shell -b "~/.local/bin/imgpaste-latest"
bind-key C-i run-shell -b "~/.local/bin/imgpaste-latest"
