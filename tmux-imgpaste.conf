# imgpaste tmux compatibility bindings.
# For the plugin, source imgpaste.tmux instead. This file expects the optional
# remote helper at ~/.local/bin/imgpaste-latest.

bind-key -n -T root C-v run-shell -b '~/.local/bin/imgpaste-latest --pane "#{pane_id}"'
