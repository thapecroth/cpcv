# cpcv tmux compatibility bindings.
# For the plugin, source cpcv.tmux instead. This file expects the optional
# remote helper at ~/.local/bin/cpcv-latest.

bind-key -n -T root C-v run-shell -b '~/.local/bin/cpcv-latest --pane "#{pane_id}"'
